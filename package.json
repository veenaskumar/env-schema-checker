{
  "name": "env-schema-checker",
  "version": "1.0.0",
  "description": "This package will check the schema of the env",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "env_checker"
  ],
  "author": "veenas",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^17.2.3",
    "express": "^5.1.0"
  }
}
